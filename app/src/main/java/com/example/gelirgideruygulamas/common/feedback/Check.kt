package com.example.gelirgideruygulamas.common.feedback

import android.widget.SearchView
import com.google.android.material.textfield.TextInputEditText

class Check(private val message: Message) {

      /*  fun amount(value:Float, safe:(Boolean)->Unit){
            val splitValue = value.toString().split(".")
            when {
                value.toString().length>8 -> {
                    message.warningMuchCharacter(8)
                    safe(false)
                }
                splitValue[1].length>2 -> {
                    message.warningMuchCharacterAfterDat(2)
                    safe(false)
                }
                else -> {
                    safe(true)
                }
            }
        }*/


}